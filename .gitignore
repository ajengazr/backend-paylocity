node_modules

# Rahasia tidak boleh masuk version control.
# Pola ini sengaja menangkap seluruh turunan .env (.env.local, .env.production,
# salinan cadangan, dan sejenisnya), karena satu berkas yang lolos sudah cukup
# untuk membocorkan kredensial database dan kunci token.
.env
.env.*
!.env.example

/generated/prisma

# Berkas log
*.log
npm-debug.log*
pnpm-debug.log*
